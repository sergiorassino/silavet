# Obsoleto: el overlay por laboratorio vive en el .env (bloque EMERGENCIA_*).
# Ver .env.example. restaurar.sh usa esas claves del .env bajado de S3.
# Este archivo solo aplica si el .env aún no tiene EMERGENCIA_APP_URL.
